-r ../shared/requirements.txt
fastapi==0.115.4
uvicorn[standard]==0.32.0
# CPU-only torch wheel keeps the image ~1 GB instead of ~6 GB for the CUDA build.
# We pin to a known-good combo for transformers 4.45.x.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
transformers==4.45.2
# ProsusAI/finbert ships as PyTorch weights only. tokenizers is pulled in by
# transformers but pin for reproducibility.
tokenizers==0.20.3
