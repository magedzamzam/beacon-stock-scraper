# ---- Beacon Screener site ----
# HTTP only by default. To enable HTTPS, generate certs into ./infra/nginx/certs/
# and uncomment the 443 server block below.

upstream beacon_api      { server api:8000; }
upstream beacon_frontend { server frontend:3000; }

server {
    listen 80 default_server;
    server_name _;
    client_max_body_size 4m;

    # Docker's embedded DNS — re-resolve service names every 10s.
    resolver 127.0.0.11 valid=10s ipv6=off;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # API — note: variable in proxy_pass forces per-request DNS resolution
    location /api/ {
        set $api_upstream "http://api:8000";
        proxy_pass $api_upstream/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /_next/static/ {
        set $front_upstream "http://frontend:3000";
        proxy_pass $front_upstream;
        proxy_set_header Host $host;
        proxy_cache_valid 200 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        set $front_upstream "http://frontend:3000";
        proxy_pass $front_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# ---- HTTPS (uncomment after placing certs) ----
# server {
#     listen 443 ssl http2;
#     server_name beacon.example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1h;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     location /api/ { proxy_pass http://beacon_api/; }
#     location / { proxy_pass http://beacon_frontend; }
# }
