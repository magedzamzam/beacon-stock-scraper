# In dev, leave NEXT_PUBLIC_API_BASE empty so requests go through the Next.js rewrite
# defined in next.config.js (/api/* -> http://localhost:8000/*).
# In production behind nginx, requests to /api/* are proxied to the api service.
NEXT_PUBLIC_API_BASE=
